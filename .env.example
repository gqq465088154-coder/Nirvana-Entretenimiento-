JWT_SECRET=replace_with_a_long_random_secret
HTTP_PORT=80
HTTPS_PORT=443
WEB_HOST_PORT=3000
API_HOST_PORT=4000
POSTGRES_HOST_PORT=5432
REDIS_HOST_PORT=6379

# ── HTTPS / Let's Encrypt ─────────────────────────────────────────────────────
# Your public domain name (must have an A record pointing to this server)
DOMAIN=example.com
# Email for Let's Encrypt expiry notifications
CERTBOT_EMAIL=admin@example.com
